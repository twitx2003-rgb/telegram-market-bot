# Design System — "Graphite" (TURA-derived, Hebrew-adapted)

מערכת עיצוב מונוכרומטית כהה, נגזרת מרפרנס TURA (Cera CY): פאנלים כמעט-שחורים
צפים על רקע גרפיט, צללים רכים ועמוקים, טיפוגרפיה גיאומטרית כבדה עם ריווח רחב,
תוויות זעירות מרווחות, מספור סקציות `.01`, ולבן כצבע ההדגשה היחיד.

---

## .01 זהות

- **מונוכרום קודם לכול.** גווני אפור-גרפיט + לבן. צבע מופיע אך ורק כשהוא נושא
  משמעות דאטה (ראו "צבעים פונקציונליים").
- **עומק במקום קווים.** היררכיה נבנית בצללים ובהפרשי-גוון, לא במסגרות.
- **טיפוגרפיה היא הגיבורה.** כותרות כבדות, מרווחות, גדולות. כל השאר שקט.
- **RTL עברית** מלא; איים LTR למספרים, טיקרים ואנגלית.
- אימוג'י: בתוויות סקציה בלבד, במשורה. לעולם לא בתוך דאטה.

## .02 צבע

### סקאלת גרפיט (הבסיס)

| Token | Dark | Light | שימוש |
|---|---|---|---|
| `--bg` | `#212529` | `#eceef0` | רקע העמוד (גרפיט) |
| `--panel` | `#101316` | `#ffffff` | פאנלים/כרטיסים (כמעט שחור) |
| `--ink` | `#0b0d0f` | `#f7f8f9` | משטח שקוע — קלט, track, קוד |
| `--line` | `rgba(255,255,255,.07)` | `rgba(0,0,0,.08)` | קו הפרדה עדין (לא מסגרת) |
| `--white` | `#f4f5f7` | `#16181b` | כותרות, מספרים ראשיים, אקסנט אינטראקטיבי |
| `--dim` | `#9aa3ad` | `#4b545e` | טקסט גוף |
| `--faint` | `#5c6670` | `#8b949e` | תוויות, מטא, ריק |

**לבן הוא האקסנט.** מצב פעיל, לינקים, hover — לבן/`--white`. אין צבע מותג.

### צבעים פונקציונליים (דאטה בלבד — חריגה מתועדת)

דשבורד פיננסי אינו קריא בלי סמנטיקת עלייה/ירידה. הצבעים הבאים מותרים
**אך ורק** על ערכי דאטה — לעולם לא על כפתורים, ניווט או דקורציה:

| Token | ערך | שימוש בלעדי |
|---|---|---|
| `--up` | `#34d399` | עלייה / שורי / קנייה |
| `--down` | `#f87171` | ירידה / דובי / סטופ |
| `--warn` | `#fbbf24` | זהירות / מעקב / דוחות |

רקעים צבעוניים: `rgba()` של אותו גוון באלפא ‎.10–.14 בלבד.
רמפות דאטה-ויז (heatmap, Fear&Greed, sparklines) יורשות את הטוקנים הללו.

## .03 טיפוגרפיה

### גופנים (התאמה עברית ל-Cera CY)

| תפקיד | גופן | משקלים | הצדקה |
|---|---|---|---|
| Display + Headings | **Rubik** | 400–900 | המקבילה העברית הקרובה ביותר ל-Cera CY: שלד גיאומטרי, פינות מעוגלות בעדינות, אישיות מדויקת-ידידותית |
| Body + UI + Buttons | **Assistant** | 300–700 | פתוח, נקי, קריא מצוין בגדלים קטנים; משלים את Rubik כמו חיתוכי הטקסט של Cera |
| מספרים / טיקרים / לטינית | **Inter** | 400–800 | ספרות tabular לדאטה; גיאומטרי מספיק לשבת במערכת |

```html
<link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;700;800;900&family=Assistant:wght@300;400;600;700&family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet"/>
```

### היררכיה מלאה

| רמה | גופן | גודל | משקל | ריווח אותיות | שימוש |
|---|---|---|---|---|---|
| Display | Rubik | `clamp(2.4rem, 7vw, 4.2rem)` | 900 | `.06em` | Hero בלבד |
| H1 | Rubik | `1.6rem` | 800 | `.02em` | כותרת עמוד/טאב |
| H2 | Rubik | `1.15rem` | 700 | `.01em` | כותרת כרטיס |
| H3 | Rubik | `.95rem` | 700 | `0` | תת-כותרת |
| Body | Assistant | `.9rem` | 400 | `0` | טקסט רץ (line-height 1.65) |
| Body-strong | Assistant | `.9rem` | 600 | `0` | הדגשה בתוך טקסט |
| Button | Assistant | `.82rem` | 700 | `.02em` | כפתורים, טאבים, צ'יפים |
| Caption | Assistant | `.72rem` | 600 | `.04em` | מטא, בי-ליין, זמן |
| Label | Rubik | `.62rem` | 700 | `.32em` | תוויות סקציה (מרווח TURA) |
| Numeric-lg | Inter | `1.5rem` | 800 | `-.02em` | מחיר/סטט ראשי, tabular |
| Numeric-sm | Inter | `.78rem` | 600 | `0` | ערכים משניים, tabular |

- מספרים — תמיד `font-variant-numeric: tabular-nums`, תמיד `dir="ltr"`.
- טיקרים: Inter 800, `letter-spacing:.04em`.
- אין להוסיף משפחה רביעית. אין italic.

## .04 עומק (Elevation)

היררכיה בצל, לא בקו:

| רמה | ערך | שימוש |
|---|---|---|
| `--e1` | `0 8px 24px rgba(0,0,0,.35)` | כרטיס במנוחה |
| `--e2` | `0 16px 48px rgba(0,0,0,.45)` | hover / כרטיס בולט |
| `--e3` | `0 24px 72px rgba(0,0,0,.55)` | Hero panel / מודאל |

- `--line` משמש כקו-עזר דק בלבד (מפרידי רשימה, תחתית nav) — לא כמסגרת כרטיס.
- Light theme: אותן רמות באלפא מופחת (‎.10/.14/.18).

## .05 צורה וריווח

- רדיוס: **8px** פאנלים וכרטיסים · **6px** קלטים · **999px** פילים וצ'יפים.
- ריווח: בסיס 4px. פאדינג כרטיס `1.1rem 1.4rem`. מקצב סקציות `3rem`.
- Container: `max-width:1140px`.
- Grid gaps: `.9–1.1rem`.

## .06 דפוסי רכיבים

**תווית סקציה (TURA)** — מספור + תווית מרווחת:
```html
<div class="section-label"><span class="sec-num">.01</span> הזדמנויות טכניות</div>
```

**כפתור ראשי** — לבן מלא על פאנל:
```html
<button class="btn-primary">צפה בפרויקט</button>
<!-- bg:var(--white); color:var(--panel); radius:8px; Assistant 700 -->
```

**כפתור משני** — רפאים: טקסט `--dim`, hover → `--white`, ללא מסגרת.

**פיל המלצה** — הצבע הפונקציונלי היחיד שמותר על רכיב:
`.rec-buy` (up) / `.rec-watch` (warn) / `.rec-wait` (faint) — טקסט צבעוני + רקע rgba.

**כרטיס** — `--panel` + `--e1`, ללא מסגרת; hover: `--e2` + `translateY(-3px)`.

**קלט** — משטח `--ink` שקוע, קו תחתון `--line`, פוקוס: קו `--white`.

## .07 כללי RTL

- מסמך `dir="rtl"`. איים `dir="ltr"`: מחירים, טיקרים, ציטוטים באנגלית,
  levels-bar, ספארקליינים.
- קו הדגשה על הקצה הימני (`border-right`).
- `margin-left:auto` במקום `margin-right:auto`.

## .08 עשה / אל תעשה

**עשה:** hover עם הרמת צל · לבן לאינטראקציה · צבע רק על דאטה · מספור סקציות.

**אל תעשה:** צבעי מותג/כחול · מסגרות כרטיס · גרדיאנטים (למעט hero עדין) ·
משפחת גופן רביעית · אנימציה מעבר ל-hover/pulse/ticker · צבע על ניווט.
